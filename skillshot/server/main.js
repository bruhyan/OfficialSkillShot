import { Meteor } from 'meteor/meteor';

Meteor.startup(() => {
  // code to run on server at startup
});


//top secret code please disable after use :>
/*Meteor.methods({
  clearUserInfo : function() {
    return UserInfo.remove({});
  }
});
*/
